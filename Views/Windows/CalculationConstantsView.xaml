<Window x:Class="NewLab.Views.Windows.CalculationConstantsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Calculation Constants"
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ثوابت الحسابات" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Constants}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="النوع" Binding="{Binding TestType}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="الاسم" Binding="{Binding ConstantName}" IsReadOnly="True" Width="150"/>
                <DataGridTextColumn Header="القيمة" Binding="{Binding ConstantValue}" Width="120"/>
                <DataGridTextColumn Header="آخر تحديث" Binding="{Binding UpdatedAt, StringFormat='{}{0:yyyy-MM-dd}'}" IsReadOnly="True" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="حفظ" Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0" Padding="16,8"/>
            <Button Content="إغلاق"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Padding="16,8"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
