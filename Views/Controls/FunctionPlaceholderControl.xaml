<UserControl x:Class="NewLab.Views.Controls.FunctionPlaceholderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="{Binding CurrentFunction.Name}"
                   FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"
                   Foreground="{StaticResource PrimaryBrush}"/>
        <TextBlock Text="محتوى الوظيفة سيظهر هنا..."
                   FontSize="16"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"
                   Foreground="Gray"/>
        <Button Command="{Binding CloseFunctionCommand}"
                Content="العودة للرئيسية"
                Style="{StaticResource MaterialDesignRaisedButton}"
                HorizontalAlignment="Center"
                Padding="24,8"/>
    </StackPanel>
</UserControl>